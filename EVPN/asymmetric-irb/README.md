# Asymmetric IRB with EVPN

The lab topology sets up a simple EVPN fabric with asymmetric IRB. Hosts use the closest PE device as the default gateway (HB1 uses L1, HB2 uses L2)

![Lab topology overview](evpn-fwd-asymmetric-irb.png)
